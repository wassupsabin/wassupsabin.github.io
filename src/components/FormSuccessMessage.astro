---
interface Props {
  id: string;
  ariaLive?: 'polite' | 'assertive';
  class?: string;
}

const { 
  id, 
  ariaLive = 'polite',
  class: className = ''
} = Astro.props;
---

<div
  id={id}
  role="alert"
  aria-live={ariaLive}
  class:list={[
    'hidden p-4 rounded-md',
    'bg-green-50 dark:bg-green-900/20 text-green-600 dark:text-green-400',
    className
  ]}
>
  <slot />
</div>